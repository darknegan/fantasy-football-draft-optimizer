# Task 4 report: artifact ingest and seeds

## Status

Complete. Artifact bios now map `top5_finish_count` and `top8_finish_count` to the required `Player` fields, and API seed data uses plausible top-5/top-8 counts plus current archetype IDs.

## Changes

- Replaced deprecated top-12 fields in the artifact loader, loader tests, primary seeds, and depth seeds.
- Replaced hardcoded `PRIME_*` categoricals with `ELITE` / `IN_THEIR_PRIME`.
- Removed all `hasPositionalTop12`, `positionalTop12`, `PRIME_WR`, and `PRIME_RB` references from `apps/api`.
- Updated deferred evaluation-engine spot checks from Task 3 so the required engine build compiles and passes.

## Verification

- TDD RED: focused loader suite failed 2 tests before the loader mapping changed.
- API data tests: 3 files, 16 tests passed.
- Evaluation-engine tests: 7 files, 80 tests passed.
- Domain, evaluation-engine, and API TypeScript builds passed.
- Changed-file Prettier check passed; changed-file IDE diagnostics are clean.

## Concern

- The checked-in schema-v4 fallback artifact is still generated by the upstream sleeperMCP top-12 pipeline. Until that producer regenerates it with top-5/top-8 fields, the loader intentionally defaults missing counts to zero.
